spring:
  profiles:
    include: secrets
  datasource:
    url: jdbc:postgresql://localhost:5447/payment-db
    driver-class-name:  org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml