
server:
  port: 8082

spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5445/driver-db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none